name: Convert HTML to APK

on:
  repository_dispatch:
    types: [build-apk]
  workflow_dispatch:
    inputs:
      html_content:
        description: 'HTML Content'
        required: true
        default: '<h1>Hello World</h1>'
      app_name:
        description: 'App Name'
        required: true
        default: 'MyConvertedApp'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Setup Java JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Gradle and Accept Android Licenses
        run: |
          sudo apt-get update
          sudo apt-get install -y gradle
          yes | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --licenses || true

      - name: Install Cordova
        run: npm install -g cordova

      - name: Create Cordova Project & Inject HTML
        run: |
          cordova create myApp com.example.convertedapp "${{ github.event.inputs.app_name || 'ConvertedApp' }}"
          cd myApp
          echo '${{ github.event.inputs.html_content || github.event.client_payload.html_content }}' > www/index.html
          cordova platform add android

      - name: Build Android APK
        run: |
          cd myApp
          cordova build android --verbose

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: converted-apk
          path: myApp/platforms/android/app/build/outputs/apk/debug/app-debug.apk
